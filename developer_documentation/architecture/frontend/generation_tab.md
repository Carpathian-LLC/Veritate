# Generation tab

## What it is

Chat-style interaction with the currently-loaded model. A typed prompt streams back token-by-token alongside per-token telemetry (FFN heatmaps, logit lens, top neurons).

## How it works

Markup at [index.html:73-500](../../../veritate_mri/web/index.html#L73). Default-active tab on page load.

- Prompt input + mode row (autocomplete vs chat vs agent vs reasoning).
- Response area renders streamed bytes from `/generate`.
- Per-token frame visualizations: FFN heatmap, logit lens table, top neurons, lens-logit, decode chain.

The submit handler POSTs to `/generate` and reads an SSE-style stream. Each event delivers a frame: `{byte, prob, lens, ffn_top, neuron_top, ...}`. Frames are appended to an in-memory `frames` array; the active frame is whatever index the scrubber sits on.

### Chat mode framing

Chat mode wraps the prompt in ChatML turn markers before sending. `wrapChat` emits the conversation so far, then `<|im_start|>user\n{prompt}<|im_end|>\n<|im_start|>assistant\n`: the same byte contract as `/hybrid/chat` and the chat corpora the SFT models train on. There is no system or persona turn: identity is trained into the model, and a persona in the prompt makes a byte model recite it every turn. Autocomplete mode sends the raw prompt, no framing. These are literal bytes, not special tokens (vocab=256).

**Conversation context.** `wrapChat` prepends whole prior turns from the transcript, newest first, until `seq * (1 - CHAT_CONTEXT_REPLY_SHARE)` bytes are used; the remainder of the context window is the reply's. Oldest turns drop first and a turn is never cut in half. Without this the model answers every question as if it were the first thing ever said to it. The wire prompt is built once in the submit handler BEFORE the new question joins the transcript, so it matches the prompt a speculative draft was built from and `prefetch_id` can still claim the buffer.

**Stopping.** A reply must never contain a turn marker of either framing, so `_chat_stop_seq` ([backends_routes.py](../../../veritate_mri/routes/backends_routes.py)) hands the engine the union (`CHAT_STOP_MARKERS`): `<|im_end|`, `<|im_start|`, `<|end|`, `<|user|`, `<|assistant|`. A plain (autocomplete) prompt gets no stop. Two details earn their place: models trained on mixed framings answer a ChatML prompt with a platform marker, so gating the set on the prompt's own framing let the engine run on into a self-conversation the client then threw away; and the markers are matched WITHOUT the closing bracket, because a byte model reproduces a marker approximately (`<|im_start|2018|`) and a mangled marker is still the model starting a turn nobody asked for. The client's `CHAT_CUT_MARKERS` mirrors the same list. `_stop_on_bytes` watches a rolling tail sized to the longest marker and emits a synthetic `stop` event whose `reason` is the marker that fired; the engine also ends the turn itself and emits TEND, so the generator is never abandoned mid-stream. `stripChatResponse` trims the finished reply at the first marker.

### Chat repetition control

Chat mode also sends repetition-control params (`rep_window`, `rep_penalty`, `no_repeat_ngram`) read from the Advanced panel (`genRepWindow` / `genRepPenalty` / `genRepNgram`, defaults 256 / 0.5 / 16, persisted). Autocomplete mode omits them, so the server default (off) applies and byte-level autocomplete repetition is untouched. Mechanism and defaults live in [../backend/inference_brain.md](../backend/inference_brain.md).

Scrubbing the timeline re-renders all telemetry panels for the selected frame. The current frame is also annotated on the decode chart.

### Layout

Top to bottom inside the prompt panel: control bar, transcript, streaming turn, composer, `#advancedWrap`.

- `.gen-bar` carries the mode picker (`#modeRow`, a `.mode-seg` segmented control), the model select, and the backend / QAT / ablation chips.
- `#chatFeed` is the transcript, rendered by `renderChatHistory` from localStorage. One `.turn` per message: `you` dashed, `model` solid, and each model turn carries `_turnStats` (bytes, b/s, mean confidence, bytes written ahead) under its text. The newest model turn gets `.turn-landed` for a one-shot entry animation.
- `#liveTurn` is the turn in flight, marked up as a `.turn.turn-model` so committing it to the transcript is continuous rather than a hand-off between panels. `_showLiveTurn(on)` shows it and stamps `.mode-chat` for chat / agent, which strips the raw-byte frame off `#response` so the reply reads as the message. Autocomplete keeps the frame, where the panel is a byte inspector.
- `#response` inside that bubble holds the generated bytes. `renderResponse` strips chat turn markers in chat / agent and renders the prompt prefix plus raw bytes in autocomplete. The blinking cursor is suppressed until the first byte, so it never appears next to `#genThinking`. `#confColorToggle` tints spans by `confidence` via `confColor`.
- `#genThinking` is the pre-stream indicator only. `_GenThink.start` runs the typewriter during prefill; the first streamed byte hides it. It never renders the answer. A claimed prefetch skips it: there is already text to show.
- `#liveStats` sits under the reply, separated by a rule.
- The `.timeline.live-scrub` bar (`#timelineRow`, live + scrubber) is `position: fixed` at the viewport bottom ([index.css](../../../veritate_mri/web/index.css)) and appears only once frames exist. Generate / stop live in the composer.
- `.composer` holds `#primeRail`, the textarea, and the actions. Enter sends, shift+enter breaks the line. `#chatClear` wipes the transcript for this device and is hidden in autocomplete.

### Answer while typing (`#primeRail`)

The composer's prime rail is the client half of [../backend/speculative_prefetch.md](../backend/speculative_prefetch.md). `#prefetchEnable` and the Settings toggle write the same `speculative_enabled` key.

A pause in typing posts the draft to `/prefetch`; the rail then shows a dot, a label, a fill bar against `speculative_bytes`, and the reply so far. Blue and blinking (`.is-running`) means the engine is still writing; green and still (`.is-armed`) means generate opens on that text. `_prefetchPollTick` polls `/prefetch` only while a draft is live and stops the moment the server reports it finished.

**When it fires.** Speculating costs a real generation, so the bar is "this person has stopped typing", not "this person paused". `_prefetchDelayMs` waits `PREFETCH_PAUSE_FACTOR` times the typist's own rolling median keystroke gap, clamped to `PREFETCH_PAUSE_MIN_MS`..`PREFETCH_PAUSE_MAX_MS`, so a fast and a slow typist both get a threshold that means the same thing to them. Terminal punctuation is the one unambiguous done signal and collapses the wait to `PREFETCH_SENTENCE_MS`. Mid-word multiplies it by `PREFETCH_MIDWORD_FACTOR` rather than blocking, because plenty of people never punctuate and their finished question ends on a letter like any half-typed one. Deleting multiplies by `PREFETCH_EDIT_FACTOR`.

Two gates suppress a draft outright: under `PREFETCH_MIN_WORDS` words, and a trailing `PREFETCH_OPEN_WORDS` entry (`the`, `is`, `of`, `what`, ...). No finished question ends on one of those, so the open-word test is what separates a long think-pause mid-sentence from an actual finish; a timer alone cannot. Replayed against typing traces at 90 / 180 / 400 ms per character, punctuated and not, this fires exactly once, at the end.

**Staleness.** The moment a keystroke makes the box differ from the question the live draft answers (`_prefetchDraft.asked`), `_primeStale` greys the rail on the spot. Waiting for the debounced tick would leave it advertising a finished answer to a question that no longer exists for up to a couple of seconds.

On submit, `_prefetchClaim` compares the request body it would send now against the one the draft was speculated for and appends `prefetch_id` only on an exact match. A claim paints the already-written bytes into `#response` (`.banked`) before the stream reopens and runs `_primeFire`, which discharges the rail. The `kind:"prefetch"` frame then reports the count, and the finished turn records it as bytes written ahead.

`_primeSync` re-arms the rail from the current page state and is called on mode change, backend change, settings apply, and the end of a generation.

### Hallucination detection (always-on) + the Hallucination panel

The analysis runs automatically on every generation `done` (when any bytes were produced) in **deferred** mode: the `done` handler calls `runHallucinationDetect(true)`, which POSTs the `frames` it already streamed to `/hallucination/analyze`, so the engine grades the exact answer that was displayed with no re-generation and no dependence on the model still being loaded (deterministic, robust). `#detectHalluc` ("re-check hallucinations") calls `runHallucinationDetect(false)` for the full re-run: it re-generates and adds the with/without-context divergence pass. There is no auto toggle. `drawHallucination` fills `#hallucinationPanel` with, in order: the prominent verdict chip + uncertain flag + confidence source; a plain-language `why` line (`_hallucWhy`) tying the verdict to the metrics; labelled metric bars (hallucination risk, grounded fraction, context divergence, confidence); the answer text with each span underlined by grounding (`.g-yes` green / `.g-partial` amber / `.g-no` red, via `_groundedAnswerHtml`) and colored by the `confidence` ramp; a color-key legend explaining the grounding underlines and the confidence ramp; a per-sentence confidence rollup (`_sentenceRollupHtml`); and provenance (grounded source chunks plus nearest training passages, labeled as similarity, not proof). The grounded/colored answer renders in the panel, not in `#response`. Backend contract and detection logic live in [../backend/hallucination_detector.md](../backend/hallucination_detector.md).

### Mode picker capability gate

`_applyModeAvailability` enables the autocomplete / chat / agent radios per the loaded model's trained tiers. `_activeCapabilities` merges both backends' capability blocks per tier (`_mergeCaps`, best status wins), since a model's trained tiers are a property of the weights, not the runtime: a chat-trained model enables chat whether the C engine or PyTorch reported `/meta`, and stays enabled if one backend idle-unloads (`setMeta` also carries caps forward). `_legacyCaps` (autocomplete only) is the final fallback for servers that expose no capability block at all. The startup `/meta` guard populates caps from a C-loaded model (`m.checkpoint || m.c_model_dir`) so chat is not greyed when PyTorch is idle.

The gate stays current without a page reload. `_activeCapabilities` prefers the selected model's entry in `_pytorchModelCaps` (filled from `/pytorch-models` at page load), so two paths keep that cache fresh: `setMeta` stamps each incoming `/meta` capability block into the cache under the loaded model's name (`pytorch_model` for the brain, `c_model_dir` for the C engine: disk-fresh reads, they overwrite a stale page-load entry), and `pollModeCaps` re-fetches `/pytorch-models` every 15s (skipped while the tab is hidden), updating caps only, never rebuilding the picker, then re-runs `_applyModeAvailability`. A chat SFT run finishing while the dashboard sits open enables the chat radio within one poll tick.

## Dependencies

- `/generate` route at [backends_routes.py](../../../veritate_mri/routes/backends_routes.py).
- PyTorch inference brain at [veritate_mri/inference/backends/pytorch.py](../../../veritate_mri/inference/backends/pytorch.py); see [../backend/inference_brain.md](../backend/inference_brain.md).
- `/meta` for the current model's layers and hidden size (drives canvas dimensions).
- Canvas helpers in [canvas_rendering.md](canvas_rendering.md).

## Pitfalls

- Frames buffer can grow large for long generations; the existing UI caps it, so check that cap before extending.
- Mode switching changes which decode strategy the backend uses. The mode value is part of the `/generate` payload.
- Chat history persists in localStorage ([state_persistence.md](state_persistence.md)) but frames do not: switching tabs and back drops the visualization.
