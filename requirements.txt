--extra-index-url https://download.pytorch.org/whl/cu128
flask~=3.1
psutil~=7.2
certifi>=2024.0.0
pytest>=7.0,<10
datasets>=3.0
pyarrow>=14.0
zstandard>=0.22

# Numpy: torch 2.2 (Intel Mac tier) requires numpy <2.0; modern torch handles 2.x.
numpy~=2.4 ; sys_platform != "darwin" or platform_machine == "arm64"
numpy<2.0  ; sys_platform == "darwin" and platform_machine == "x86_64"

# Torch: Intel Mac caps at 2.2 (last with x86 macOS wheels). Everything else
# rides modern torch.
torch~=2.11 ; sys_platform != "darwin" or platform_machine == "arm64"
torch~=2.2  ; sys_platform == "darwin" and platform_machine == "x86_64"
