# Extra dependencies for the experimental market platform (veritate_mri/market/).
# Kept separate from the canonical Veritate requirements.txt so the market platform's
# deps never bleed into the core product. Install:  pip install -r veritate_mri/market/requirements.txt
# (pandas + numpy are already required by canonical Veritate.)
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
certifi>=2023.0
