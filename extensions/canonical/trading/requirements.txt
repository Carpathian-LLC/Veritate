# Extra dependencies for the Trading extension (extensions/canonical/trading/).
# Kept separate from the canonical Veritate requirements.txt so the extension's deps
# never bleed into the core product.
# Install:  pip install -r extensions/canonical/trading/requirements.txt
# (pandas + numpy + torch are already required by canonical Veritate; the byte model uses those.)
# certifi is the only extra: live.py uses it for Binance.US REST over TLS.
certifi>=2023.0

# scikit-learn + joblib: the ml7 strategy trains and loads its frozen HGB ensemble.
scikit-learn>=1.4
